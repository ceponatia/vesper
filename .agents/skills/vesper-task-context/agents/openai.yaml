interface:
  display_name: "Vesper Task Context"
  short_description: "Prepare compact fresh-context task briefs"
  default_prompt: "Use $vesper-task-context to prepare a compact, verified brief for a fresh agent or resumed session."

policy:
  allow_implicit_invocation: true
