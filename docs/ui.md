# UI

Next.js App Router pages + React 19 + Tailwind 4. Aesthetic: quiet dark "reading room" — ink background, warm paper accents, serif narrative type (`Source Serif 4` or similar variable font), sans UI chrome. Design tokens in `globals.css` `@theme`; no component library — small owned primitives in `src/components/ui/`.

## Pages

```
/                        Dashboard: conversations lead ("Continue talking to…" — D12; hidden until a chat exists), then the cast strip
/worlds                  Successor-engine front door (SuccessorWorldsPage): create and open successor-engine chats, each in its own fresh world (engine.rollout.plan.md; replaced the deprecated world-model library)
/characters              Library grid
/characters/forge        Prose prompt → draft review → save
/characters/:id          Editor: profile · attributes (registry picker — single-open accordion, all sections collapsed initially, every applicable attribute shown blank-or-set; body-config/features overrides sit at the bottom of the same accordion) · personality (voice/presentation/movement) · disposition · outfit · portrait studio · chat. Header carries **✦ Forge the rest** (fill every empty field from the sheet, never overwrites — saves first, result lands unsaved for review); each content tab carries **↻ Re-draft tab** (rewrite that tab narrator-formatted from the whole sheet; manual attribute/trait values kept, conflicts reported), and the Attributes tab adds **◉ From portrait** once a ready avatar exists (vision fill-blanks pass; [authoring.md](authoring.md) §In-sheet forge) (a **summary surface** since the standalone arc — playing happens on /chat/:chatId, see §The conversation page: a **Chat defaults** card — the **narrator model** dropdown (curated `lib/narrative-models.ts` list; picking saves immediately to `characters.chatModel`, serialized against rapid picks, and the value is hoisted to the edit page so it survives tab unmounting) plus the authored **Starting Relationship** stage + one-line note (`profile.playerRelationship`, rides the editor SaveBar) — and a **Conversations** card listing this character's active chats (title-or-auto, `timeAgo` stamp, last-line snippet, each linking to /chat/:id) with a **New conversation** button opening the shared components/chat/new-chat-dialog.tsx)
/personas                Library grid (the player as a library entity — cards show the per-owner-unique `title`, with the in-fiction `name` on the subtitle line)
/personas/:id            Editor: profile (title · name · tags · species/heritage · bio · voice · what-you-like) · body (the shared registry AttributePicker, incl. intimate-region body config) · wardrobe (the shared OutfitEditor). No forge, portrait studio, clone or publish — a persona is yours and unshared
/chat                    Chats hub: conversation rows (portrait, name/title, snippet, mood + stage chips, relative time, and a "has something to say" accent dot — ChatSayMarker, derived at read time from the row's top open loop, or with no loops the newest milestone unseen since the chat was last opened (the milestones_seen_at cursor, stamped on conversation mount — §8.4 v2), no jobs and never the wall clock; tapping it opens /chat/:id?say=1), Active/Archived shelves, rename/archive/delete actions, "New conversation" (components/chat/chats-page.tsx; ?new=<characterId> opens the dialog pre-picked — the library-card entry point). The dashboard's conversation rows carry the same marker.
/chat/:chatId            Full-screen conversation (mobile-first; §The conversation page)
/locations, /locations/:id   Lean library + editor (Details · Image tabs)
/items, /items/:id           Lean library + editor (Details · Image tabs)
/gallery                 Tabbed image hub — Scenes · Portraits · Entity art — chat-scene grouping, avatar chip filters, favorites, multi-select delete, keyset "Load more" (components/gallery/gallery-page.tsx; images.md §Gallery)
```

The global header (`components/shell/app-shell.tsx`) links **Chats · Worlds · Library · Gallery** at ≥md (library-ux.plan.md §Follow-up pass). **Library** is one nav entry spanning the five collection routes — `/characters` (its landing tab), `/personas`, `/locations`, `/items`, `/social-cards` — which keep their URLs and share a tab strip inside the library shell (`LIBRARY_TABS` in `entity-library.tsx`; Worlds stays its own destination, no strip); the nav entry lights for any of them via `NavDest.match` (`nav-links.ts`, `isNavActive` takes the dest). **Worlds** now points at the successor engine's front door (`/worlds`, `SuccessorWorldsPage`), not the retired world-model library. On phones a bottom tab bar or hamburger drawer takes over (see **Mobile**). The Gallery is a separate page; scene images never appear on the landing dashboard.

The character portrait studio (`components/characters/portrait-studio.tsx`) generates the canonical avatar from attributes, accumulates Venice pose/outfit/expression/setting variants, and promotes any variant to canonical. Its **Portrait history** grid keeps non-canonical avatar attempts and variants visible; failed rows render as error cards using `images.meta.error` so provider failures do not vanish after polling. **Upload image** opens the crop dialog (`avatar-upload-dialog.tsx`): a modal whose helper text states the 768×1024 (3:4) target, then — unless the chosen file is already 3:4 — lets the user drag to reposition and a slider/scroll to zoom inside a 3:4 crop window before the framed region is scaled, saved, and set as the avatar (synchronous, demo-safe — [images.md](images.md) §Avatar upload).

The **persona** editor (`components/personas/persona-editor.tsx`, persona-library.plan.md) is **Profile · Body · Wardrobe** — three tabs against the character editor's eight, because a persona has a body, a wardrobe and a bio but no personality, disposition, drives, relationships or schedule (the narrator never writes the player's lines). It **embeds `AttributePicker` and `OutfitEditor` unchanged** — both are pure props-in/callback-out over contract shapes with no `characterId` coupling — and the species/heritage re-seeding rule is shared with the character editor via `speciesChangePatch`/`heritageChangePatch` (`components/characters/attribute-helpers.ts`), not copied. Its Profile tab is the one place a **title collision** surfaces: `(owner_id, title)` is UNIQUE, so a duplicate title returns a typed 409 that renders **inline on the Title field** (not a toast) and holds the draft dirty; autosave pauses until the title changes, since retrying the same conflicting title every 1.5s would only churn 409s.

The library **item** and **location** editors are split into **Details · Image** tabs (`components/ui/tabs.tsx`). The item Details tab shows a **Type** select when the picked clothing category has a subtype vocabulary (jewelry/headwear/eyewear — [contracts/items.md](contracts/items.md) §Clothing subtypes); picking one pre-fills coverage like a category template, and cards/rows chip the type instead of the broad category. The Image tab is a minimal studio (`components/library/entity-image-studio.tsx`, shared by both): a Generate/Regenerate button and the current image with click-to-enlarge — no variants, no upload (regenerate if you dislike it). Generation runs as a background job and the tab polls until it lands, so leaving the page never interrupts it. Items get a 1:1 product photo; locations a 3:2 establishing shot whose type follows the location's scale ([images.md](images.md) §Entity images). Every lean library editor (items, locations, social cards — every branch, including loading/error) opens with a **`← <Library>` back link** (`components/library/back-link.tsx`, `.touch-target` on phones) returning to its grid — the mobile escape hatch; the grid restores the toolbar state the user left (below).

The library **location** editor's Details tab carries the full location design (parity with the world map tab): **Scale**, **Area**, and a **Connections** section — undirected links to other library locations added through the shared `EntityPickerDialog` (search + thumbnails), shown as removable tags. Connections persist as `location_links` and propagate into a world's map when the linked set is imported ([database.md](database.md), [images.md](images.md) §Entity images). A location's scale also drives its generated image (landscape vs interior).

The shared library grid (`components/library/entity-library.tsx`) is config-driven per entity; the **Items** library is the full faceted browse ([library-ux.plan.md](developer-notes/finished/library-ux.plan.md)):

- Segmented **Clothing · Object · Container** buckets (live counts; no "All" — the page opens straight into Clothing, the closet view), then **registry-backed facet chip rows** that adapt to the bucket — clothing gets category / wearer / layer chips + color **swatches**, objects get subtype chips (`components/library/item-facets.ts`; wearer follows `wearerMatchesFilter`, so absent/unisex garments match every wearer chip). Facets filter the loaded set client-side with counts scoped to the other active filters; options absent from the data are hidden. Switching buckets clears facet picks.
- With nothing narrowing, the grid renders **grouped sections** — Clothing → by category (the "closet" view), Object → by subtype; any search/tag/facet goes flat. A single section renders flat too (one group is noise, not organization). (The `buckets.defaultId` config also suppresses "All" and sets the initial bucket — items is the only entity currently using it; social-cards still opens on "All".)
- A **grid ⇄ list toggle** (persisted per entity in localStorage) swaps the rich cards for compact rows — small thumb, name, structured chips (category/subtype + color dot), tags — for hundreds-of-items scale; plus a **sort** select (Recent first / By name, applied server-side).
- The rest of the toolbar state (bucket, scope, sort, search, tags, facet picks) persists **per entity in sessionStorage**, restored through the lazy state initializers on mount — so opening an editor and coming back (its back link, the nav, or browser back) lands on the same view. Stored buckets/facet options no longer in the vocabulary are dropped at read time (a stale pick would silently empty the grid), and **New** creates in the active bucket — a blank clothing item from the Clothing view, a taboo card from the Taboo tab (`config.create` receives the bucket; "All" falls back to the schema default).
- Free tags moved behind a **Tags** panel: multi-select chips (comma-joined into the `tag` param, ANDed server-side via jsonb containment), with a filter input past 15 tags. **Machine tags** (`suggested`, `seed:*`) are hidden from every filter UI and card chip row (`lib/tags.ts`); the data keeps them.
- Characters and items get a hover-revealed **Duplicate** action on cards/rows (the `config.clone` seam → the kind's `/clone` endpoint; works on public entries too — copy-on-use) that opens the copy's editor; the character edit page header and the item editor SaveBar carry the same **Duplicate** (save-first, like the Forge). Wardrobe near-variants ("same top in three colors") and archetype characters start here (ux-improvements.plan.md slice 1).
- **New** creates the entity immediately with a **randomized placeholder name** ("Untitled item k3f7" — concurrent drafts never collide) and routes to its editor: create-on-new means a draft can never be lost before its first save (ux-improvements slice 7).
- **Editor autosave** (`components/hooks/use-autosave.ts`, slice 7 — ruled): the character and item editors save silently ~1.5s after the last change, and immediately when focus leaves a field (`onBlur` on the editable container — free text lands on field exit, never mid-typing); the Save button stays as a loud manual flush, and `beforeunload` warns only while something is unsaved/in flight. **Forge-draft discipline**: a staged ✦/↻/◉ result pauses autosave until the manual Save (the SaveBar stays the review step). Advisory inline validation (never blocking): empty Name, a drive with a blank want, a schedule row with a blank activity ("this row is dropped on save").
- **Attribute accordion summaries** (slice 7): each collapsed section header previews its set values ("auburn, shoulder-length, wavy", falling back to "N set" when long) with a distinct italic *empty* for unauthored sections — fully-authored vs empty is scannable without expanding, and Forge-the-rest / From-portrait output is reviewable at a glance.
- **Generate images** (Items/Locations) is unchanged: ids visible under the active filter that lack an image → confirm dialog → background batch, grid polls, hidden on an empty library (UX-audit P3). Items adds **Organize** beside it — the facet classify backfill: visible items missing a facet go to `POST /api/items/classify`, a background `item_classify` job fills **only absent** category/layer/wearer/color/subtype fields from name+description via a cheap model (`server/api/item-classify.ts`), and the grid polls as chunks land. Present values are never overwritten, so it's always safe to press again.

Since the follow-up pass, the **other libraries facet too** (`components/library/library-facets.ts`, on the same `FacetDef` machinery — which grew an optional multi-value `values` accessor): **characters** get species / gender (the natal-sex `…_born_*` variants collapse into one browse chip), **locations** get scale, **social cards** get severity-tier + trigger-concept chips — with matching card chips (a character's notable species, a location's scale, a card's tier + first triggers) and the grid⇄list toggle on all of them. The list payloads carry the facet columns explicitly — `/api/characters` SQL-extracts `speciesId` + the `identity.gender` attribute value from the profile jsonb; `/api/locations` ships `scale` — keeping the explicit-columns posture rather than shipping whole jsonb blobs.

Server-side, the items list API takes the same facets as query params (`category`/`subtype`/`layer`/`wearer`/`color`, plus `sort` and comma-ANDed `tag`) — applied **before** the result cap in `searchLibraryIds` (the defaultOutfit truncation lesson), which is what the pickers lean on. List endpoints select explicit columns (the bare rows used to ship the 1536-dim search embedding to the browser). **Every shareable kind honors `?scope=all|public|owned` and `?sort`** (the auth.plan.md scope fast-follow closed in the follow-up pass — scope was previously dropped client-side for characters/locations/items, and their routes ignored `sort`); the row SELECTs widen to owner-or-public so the discovery scopes can actually return another owner's published rows.

**Pickers** (`components/library/entity-picker.tsx`, replacing the old `library-picker`): `EntityPickerDialog` is the one search-and-pick surface behind every "add from library" flow — debounced server search, thumbnails, per-row structured chips, optional **facet chip rows** that re-query, a **grouped empty-query browse** (clothing by category), and a **multi-select basket mode** (pick several, confirm once). Call sites: the character **outfit tab** (below), location connections, and the social-card import. The **new-chat dialog** keeps its bespoke avatar grid but its filter input now re-queries the server (`q`) instead of narrowing the capped first page (`components/hooks/use-debounced-value.ts`).

The character **outfit tab** (`components/characters/outfit-editor.tsx`) is a **preset switcher over wardrobe slots** (ux-improvements slice 8 — `profile.outfits` **replaced** `defaultOutfit`; legacy rows lift into an "Everyday" preset at parse): named looks (casual / work / date night / sleep) as selectable chips — the **first preset is the default** (what the forge targets, the avatar wears, and a fresh chat starts in) with add / rename / duplicate / make-default / delete controls — and the selected preset's items edit as wardrobe **slots** — Tops & dresses · Bottoms · Underwear · Footwear · Accessories (`lib/clothing-slots.ts`, a pure category→slot map; unresolved/uncategorized references land in "Other") — each slot's **+ Add** opening the EntityPicker pre-filtered to that slot's categories **and the character's wearer target** (from `identity.gender` via `wearerHintForGender`; always overridable in the picker's facet chips), so a long clothing library arrives as "the tops that fit her". Empty slots read "Nothing worn." A trailing "+ Add from the whole wardrobe…" opens the unscoped picker. The Profile tab's Daily-rhythm rows gain a **Wearing** preset pick (rhythm auto-dress — a pickup skip landing in that window dresses the character for it), and since chat-wardrobe-parity the chat Character sheet carries a **structured Wardrobe editor** (`components/characters/chat-wardrobe-editor.tsx`) reusing this same slot + picker design over the conversation's worn item ids (§The conversation page).

## The conversation page

`/chat/:chatId` (`components/chat/chat-conversation.tsx`, [character-chat/](character-chat/README.md)) is the full-screen play surface: a mobile-first single column filling the full viewport (`h-dvh`) below `md` — the global app header is suppressed on this route at that width (mobile-ux.plan.md W1), so this page's own header is the only chrome — and reverting to `calc(100dvh - 3.25rem)` at `md`+ once that header returns (see the app-shell.tsx comment), transcript scrolling internally, composer pinned to the bottom edge with iOS safe-area padding (the bottom tab bar is suppressed on this immersive route at every width). It is **chatId-driven — the conversation always exists** (created from the Chats hub / New-conversation dialog); there is no lazy create. One `GET /api/chats/:chatId` envelope settles the screen: transcript + chat header (title, archived) + character card (name, `avatarImageId`, `chatModel`). The pieces:

- **Header row**: back link to `/chat`, small portrait (tap → the portrait lightbox), character name + chat title (auto-title = the character's name when the title is empty), and a **menu** — a bottom `Sheet` under md, an AccountMenu-style popover at ≥md — holding the **narrator model** select (same curated list + serialized save-on-pick as the editor tab), **Scenario setup**, **Relationship** (opens the panel below), **Character sheet** (the primary's per-character sheet — followups ruling 13; tapping any roster member's name opens THEIR sheet, same component, `?characterId=`-targeted: axes + texture toward the player, meters, conditions, mind note, loops, a **structured Wardrobe editor** (chat-wardrobe-parity — `components/characters/chat-wardrobe-editor.tsx`: a preset switcher that dresses her in a named look + per-slot **equip/remove** over the worn item ids, reusing the outfit-tab's slot + EntityPicker + `itemsApi` primitives, plus the free-text overlay for ad-hoc garments and the exposed-flag fallback used only when nothing is equipped), presence toggle at roster > 1; the engine readouts — pulse trace, cue-split, memory trace, chat clock, next-turn memory queries — are **admin-only** since ux-improvements slice 4, same role check as the Inspector), an **Inspector** item (admins only — navigates to `/chat/:chatId/inspector`, the memory inspector; its `/api/admin/chat-inspector` family is role-gated server-side so it works on the deployed build, [character-chat/api.md](character-chat/api.md) §API surface), a **"Let time pass"** chip row (Moments/Hours/Overnight/Days → `POST …/time-skip`, hidden on archived chats), **Rename** (PATCH `title`), **Archive/Restore**, and **Delete chat…** (confirm dialog → hard delete → back to `/chat`).
- **Scene images**: a collapsible section under the header holding the scene strip, **collapsed by default at every width** — the toggle is remembered in component state only, and the section heading lives on the disclosure toggle (the strip's own header row is the Generate button plus a **scene-model dropdown** — save-on-select through the state PATCH, serialized against rapid picks like the narrator pick; "Avatar reference" is the identity-locked default, the t2i models are style swaps rendered without the avatar, [images.md](images.md) §Scene images). The conversation page owns **one** scene fetch/poll shared by the strip and the inline transcript moments; `SceneStrip` is presentational over that list (it renders + queues, never fetches). The poll arms while the GET's **`rendering`** flag is true (a live `chat_scene_image` job) **or** a pending row exists — the flag is what covers the slow composer step before the pending image row lands, so a queued render's "Painting…" tile resolves live instead of sitting until a manual refresh (and an auto-queued or mid-compose-refreshed render still shows the tile, via `rendering`, with no local click state).
- **Standing portrait**: at ≥`lg` the character's `AvatarPanel` portrait sits in a fixed column to the **left of the transcript** (using the desktop side real estate; `w-52`, `w-64` at ≥`xl`), clickable to enlarge. Below `lg` the column is hidden entirely — phones reach the full-size portrait by tapping the **header portrait** or any reply's **circular avatar** (`MessageBubble`'s optional `onEnlargeAvatar`, passed only when an avatar image exists). All three affordances open the **same** shared `ImageLightbox`, one instance at the page level.
- **Privacy mode** (mobile-ux.plan.md ruling 4): a client-side toggle, persisted in `localStorage` (`vesper:privacy-mode`, default off — `components/hooks/use-privacy-mode.ts`), for keeping the screen safe to have open around company. On: the standing portrait collapses to nothing (page background, no placeholder chip) leaving a quiet eye-off toggle in its place; the header portrait and every feed avatar swap the image for a first-initial monogram (`EntityImage`'s `privacy` prop — deliberately distinct from the missing-image fallback) and stop opening the lightbox; the scene-image disclosure, its strip, and inline scene-moment thumbnails are hidden entirely, so no scene lightbox is reachable either. The primary toggle sits on the standing portrait (desktop-only real estate); the conversation **⋮ menu** carries the same toggle with an on/off label so phones, which have no left aside, can reach it too — `chat-conversation.tsx` owns the one boolean and passes it down to every consumer as a prop.
- **Right aside — the clock card** (chat-clock-calendar): a mirror column on the **right** of the transcript (same `hidden lg:flex` breakpoints/widths), first tenant the **Story time card** (`components/chat/chat-clock-card.tsx`): weekday + date (year omitted), clock time + day-part + "day N", and the four **skip chips** (Moments/Hours/Overnight/Days) right beside the display that makes them legible — each chip's tooltip previews its landing ("→ Friday evening"), and the skip toast names where time landed. Tapping the date opens the **"Story starts on…"** editor — a themed calendar-grid picker (`CalendarStartDialog` over `ui/date-picker.tsx`: weekday column headers, ‹ › month paging, a live weekday-first summary line, and on desktop a typed date field that parses free-form input) → `ChatStateEdit.calendarStart`; rebasing re-derives every displayed date, and the year exists only to pin weekday alignment. Scenario setup opens the same dialog from a "Story starts" row, so the anchor has two entry points; on phones, where the aside is hidden, that's the only one — the **Roster sheet no longer shows the clock at all** (mobile-ux.plan.md rulings 1–2). The pickup strip's skip chips carry the same landing tooltips. Below the clock, on **successor-engine chats only** (`/worlds` front door), sits the **World card** (`components/chat/chat-world-card.tsx`, world-ui.plan.md slice 1): a "Where you are" panel showing the player's place ("At home" / "At the town square") or in-transit line ("Walking to the town square — there in ~4 min"), each cast member's whereabouts ("Nora — here with you", "Sable — at the town square"), and **destination chips** in the skip-chip idiom (`Button size="sm" variant="quiet"` + a `~5 min walk` caption row). A tap is **skip-style travel** (ruling 20): the server composes `move` + a drain to the journey's earliest arrival, the clock jumps the walk, a toast names the landing in skip-parity, and the card + state refresh. A refused move renders the §14.4 public face (publicReason + legal-alternatives) inline — the first real UI consumer of that shape. The card renders nothing for a legacy/degraded chat (`world === null` from `GET …/world` — ruling-18-style affordance hiding). Because the desktop aside is hidden below `lg` and the world card is the **only** travel surface, it is also folded into the phone **Roster sheet** (unlike the clock — the world card has no other mobile home).
- **Transcript**: the same `MessageBubble` hover actions as before — **Edit** / **Delete** (poisoned-window recovery levers; both now also reconcile the line's extracted memory, [character-chat/pipeline.md](character-chat/pipeline.md) §Post-turn fan-out), **Rerun** on the user's own lines (re-sends that prompt, dropping everything after it and cancelling any in-flight reply; the server still drains/persists the cancelled reply and the transcript reload reconciles), and **Remember** (opens the "Remember this" dialog prefilled with that line — a pinned player fact, [memory.md](memory.md) §Pinned facts). Since slice 5: **Another take** on the last settled reply (streams the regeneration into the same bubble in place — state rolls back to the pre-exchange snapshot and the old take's memory is retracted server-side), a **takes pager** (`‹ 2/3 ›`) in the footer of any reply with recorded takes (switching is display-only — state/memory reflect the last *generated* take), and a subtle **stopped** chip on replies the player cut short. Since slice 9: ready scenes anchored to a message render as **inline scene moments** — a thumbnail row (`SceneMomentRow`, shared lightbox) under the line they illustrate; un-anchored/dangling-anchor scenes stay strip-only. On **successor-engine chats**, a `MessageBubble` branch also renders **world beats** (world-ui.plan.md slice 2) — a muted, centered, non-bubble system line for travel / time-skip / scene-ended events ("You walk to the town square. · <landing>", "Time passes — it's now <landing>."). A beat is an ordinary transcript row (`role: "assistant"`, `meta.worldBeat = { kind }`, the phrased text on `content` stamped through the shared `formatSimLanding`); it replaced slice 1's travel/skip **success toasts** (the transcript reload after the command surfaces it), while the §14.4 **refusal** face on the world card stays. Legacy-lane skip toasts are unchanged. Message bodies on **both** sides render through the **markup span renderer** (`components/characters/message-content.tsx` over the pure, unit-tested `message-markup.ts`, which reuses `lib/message-spans` — no second parser; [developer-notes/player-input-perception.plan.md](developer-notes/finished/player-input-perception.plan.md) slice 5): `*thoughts*` and `_italic_` show italic with the sigils hidden, `*Name: …*` comms read as a text message (the `Name:` label kept, the body italic), and `((OOC))` gets an out-of-fiction amber aside (parens hidden) — speech (quotes kept) and narration render exactly as before, blank-line paragraph breaks survive, and an unterminated sigil mid-stream stays literal until it closes. Because the outermost sigil wins, a `_…_` pair nested **inside** a span body (most commonly quoted speech — `"it's _perfect_!"`) never becomes its own span; every piece body therefore also flows through `parseEmphasisRuns` (`lib/message-spans`, unit-tested), which italicizes the run with the underscores hidden (and flips it upright inside an already-italic body — thought/comms/OOC — standard nested-emphasis typography). The exported transcript keeps the **raw** sigils (markdown-compatible). Narrator/character replies (not the player's own bubbles) additionally render as **in-bubble per-speaker segments** (dialogue-attribution — `chat-segments.ts` over `lib/segmenter`, unit-tested): the `[Name]` tag is hidden behind a small accent speaker label, and in this one-on-one an untagged **whole-line quote** attributes to the character just like a tag would — segments stay INSIDE the one reply bubble (the chat lane is "a story being told", not separate bubbles), segment content still flows through the span renderer, and a segment that is solely a `*Name: …*` comms line keeps its comms label rather than doubling it. Re-parsing the growing reply each streaming render is cheap (the parser is pure) and the unterminated tail stays stable. The transcript's auto-scroll is **stick-to-bottom** (`lib/scroll-pin.ts`): it pins on new lines only while the reader is already at the bottom (scrolling up to reread stops the yanking, and surfaces a **"Jump to latest" pill**), and a `ResizeObserver` on the content column re-pins as async content (scene thumbnails, avatars) grows it after load — without that, the initial view landed mid-transcript once images finished, hiding the newest exchange below the fold. The GET returns the newest **100-row page**; a **"Load earlier"** button at the top of the transcript keysets older pages (`?before`, [streaming-api.md](streaming-api.md) §Pagination) with the viewport held in place via a prepend anchor (never yanked; the ResizeObserver is suppressed during the restore frame). A player-initiated exchange resets to the newest page and re-pins. A stream that settles cleanly but delivered **zero tokens** (the server's first-token watchdog tripping on a stalled provider — no reply row persists) surfaces an explicit error toast ("<name> didn't reply") instead of the pending bubble silently vanishing.
- **Bottom cluster**: a slim scenario-premise line (tap → Scenario setup), the status strip (an **ambient story-time chip** — weekday-first, "Fri · 2:10pm", tap → Scenario setup, always visible during play so the player can't silently disagree with the narrator about what day it is — mobile-ux.plan.md ruling 3 — plus mood chip + affinity stage chip + off-baseline meter pips + a read-only **outfit chip** — compact garment summary via `outfitSummary` over the snapshot's resolved `outfitLabel` (the rendered worn garments + overlay since chat-wardrobe-parity; falls back to the free-text `outfit` for legacy chats), tap to expand to the full phrase with an `· exposed` marker when set, hidden when the outfit text is empty; editing stays in the Character sheet — ux-improvements slice 3; the roster panel shows the same per-member outfit as a small line under each name, carried on the GET envelope's roster rows), a dismissable **"Pick up:" strip** on reopen (`ChatPickupStrip` — Continue is the no-op default; Later / Next morning / Days later fire a time skip), the one-tap **"Let _name_ speak" banner** when the page opened via a `?say=1` marker tap (sends a `continue` beat cued to exactly that open loop; with no loops standing — a milestone-keyed tap, §8.4 v2 — it runs the full initiative opener instead; the param stripped after read), the **Prompt _name_** opening-beat button (empty transcript only) and a quiet **Go on →** (idle, last line an assistant reply — a `continue` beat: no user bubble, the pulse is skipped, the archivist still remembers it), the **action chips** (chat-action-beats.plan.md — "Offer a drink / Freshen up / Take a breather / Heat things up"; each carries a tooltip `hint` saying what the tap does, and a tap is now a narrated `action_beat` beat: the character plays a small beat while the chip's deterministic state effect applies pre-narration, no player bubble, disabled while a reply streams, the tapped chip showing a busy spinner), then the composer (Enter sends, Shift+Enter newline; a **bookmark button** opens the same "Remember this" dialog empty; **Stop** swaps in for Send while a reply streams — what already streamed persists as the reply). A stage-change toast fires when the affinity stage moves. The composer carries an **OOC assist** (player-input-perception slice 5): typing `((` auto-inserts the closing `))` with the caret left between them (Backspace on the empty pair removes both rather than stranding the `))`; skipped when `))` already follows, and paste is untouched — only a literal `(` keypress triggers it), and while the caret sits inside a `((…))` block the textarea tints amber with an **OOC** badge cueing that it's a note to the storyteller no one in the scene hears. Caret placement after the auto-insert is deferred to the next animation frame (the controlled-textarea pattern); the in-block check is a cheap bracket-balance probe (`caretInOocBlock`), since the parser is offset-free.
- **Relationship panel** (`components/chat/chat-relationship-panel.tsx`, in the shared `Sheet`): stage + affinity, the history **sparkline**, **milestones** (glyph-badged by kind, newest first), unfinished business (open loops), the **story so far** (rolling summary) with a **Rebuild from full transcript** lever (`POST …/summary/rebuild`), and **Export** download links (md/json, with a memory-appendix checkbox). One `GET …/relationship` settles it.
- **Archived** conversations render read-only: a slim "Archived — restore to continue" banner with an inline Restore, composer/chips disabled (the server also 409s `chat_archived`).

The **Scenario setup** modal holds only the CHAT-WIDE fields since followups ruling 13 (premise, the setting-wide house-rule cards, the **"Auto-generate a scene at big moments"** toggle — writes `sceneAuto` `"off"|"milestones"`, [images.md](images.md) §Scene images — and a **scene-model** select mirroring the strip's dropdown); outfit + exposure moved to each member's Character sheet. It carries the scenario-preset loop (spec §1.5): an **Apply preset** select (fills the chat-wide draft; Save persists) with per-preset delete, and **Save as preset…** capturing the draft + the primary's current outfit/relationship (per-character seeds, applied only when a NEW conversation starts from the preset); the **New-conversation dialog** offers **Start from preset**, seeded server-side at create.

The editor's Chat tab links here rather than embedding any of this (see `/characters/:id` above).

## Mobile

One responsive component tree (no separate mobile build); CSS breakpoints reflow the layout and a few JS hooks switch behaviour. There is **no user-agent sniffing** — "mobile" is screen width: `useIsMobile()` (`components/hooks/use-is-mobile.ts`, `matchMedia("(max-width: 767.98px)")`, one hair under Tailwind `md`/768) drives behaviour, `md:`/`lg:` utilities drive layout. The root layout exports a `viewport` with `viewportFit: "cover"`; the bottom bar and chat composer reclaim the iOS safe area with `env(safe-area-inset-bottom)`.

**Primary nav** (`app-shell.tsx`): at ≥md the horizontal top-nav shows. Below md it swaps to one of two chromes chosen by a persisted **nav-mode** (`lib/nav-mode.ts`, default `tabs`): a fixed **bottom tab bar** (`bottom-tab-bar.tsx` — Home · Chats · Library · Worlds + a **More** bottom-sheet holding Gallery and the nav-style switch; Chats holds a primary slot per D12, Library covers the four collection routes via `NavDest.match`) or a **hamburger + left drawer** (`nav-drawer.tsx`). `NavMode` is a two-value union, so exactly one chrome ever renders — "both" and "neither" are unrepresentable; the switch (`nav-mode-toggle.tsx`) lives in both the More sheet and the drawer footer so you can always flip out of the current mode. The bottom bar is **suppressed on the immersive surface** (`/chat/:chatId` — not the `/chat` hub) so its fixed footer never collides with the composer; the hamburger lives in the header and stays available everywhere.

**Sheet** (`components/ui/sheet.tsx`): an edge-anchored modal (`side` = right/left/bottom) that shares Dialog's overlay-click/Escape/focus-trap via the extracted `components/ui/use-focus-trap.ts` (Dialog uses it too). The trap focuses the panel **only on the open transition** (its own effect, keyed on `open` alone, skipped when focus is already inside — so an `autoFocus` child wins): callers pass inline `onClose` functions, and an effect that depended on `onClose` *and* called `focus()` re-ran on every parent render, yanking focus out of the input being typed in (one keystroke per click — the new-chat title bug). It backs the nav drawer, the More overflow, and the conversation page's menu / relationship / character-sheet sheets. Its `fixed inset-0` overlay is **portalled to `document.body`** (via `createPortal`): the nav drawer mounts inside the `backdrop-blur` header, and a `backdrop-filter`/`transform`/`filter` ancestor establishes a containing block for `position: fixed` descendants — without the portal the overlay collapsed to the header's height and the drawer body (links + the nav-mode switch) was clipped to a sliver, leaving an apparently-empty hamburger with no way back to the bottom bar. `document.body` is always a clean viewport-sized containing block.

**Touch** (all in `globals.css` under `@media (pointer: coarse)` / `(hover: hover)`): small controls opt into a ≥44px tap height with `.touch-target`; reveal-on-hover action clusters use `.hover-reveal` (always visible on touch, hover-gated only where hover exists — e.g. portrait-studio variant actions); form fields are pinned to 16px on coarse pointers so focusing them never triggers iOS zoom. The dense `Tabs` strip (the 5-tab world / 7-tab character editors) scrolls horizontally and keeps the active tab scrolled into view.

## Conventions

- Server components fetch; client components interact. Data flows in through route handlers already shaped for the view (no client-side joins).
- Client data layer: plain `fetch` + small hooks (the chat conversation's transcript/stream hooks); no react-query unless pain demands it.
- Every async surface has a skeleton + error state; images always have the monogram fallback; missing/legacy data renders as absent, not broken ([resilience.md](resilience.md) §7).
- Secondary-text tokens must clear **WCAG AA** on the dark surfaces they sit on: the faint `--color-paper-500` is the AA floor (≥4.5:1 on `ink-800` cards and `ink-700` raised surfaces) — don't reintroduce a dimmer body-text grey (UX-audit M6). On top of that floor, an **opt-in high-contrast theme** (header toggle, `components/shell/contrast-toggle.tsx`) sets `data-contrast="high"` on `<html>`, which `globals.css` keys a `:root[data-contrast="high"]` block off to lift the quiet `paper`/`ink` tokens further; the choice persists in `localStorage` and is applied **pre-paint** by an inline script in the root layout (no flash). The pure read/parse/apply path is `lib/contrast-theme.ts` (UX-audit §4 / feature #6).
- Forms: controlled state + zod validation on submit; shared `Field` primitives; sticky save bar with dirty-state indication on long editors (the old `EditPageChrome` pattern, rebuilt). Editor drafts seed from fetched data **exactly once per entity** (`components/hooks/draft-seed.ts`, applied during render): refetches and silent reloads never clobber in-progress edits, and navigating between entities drops the stale draft until the new one loads. The chat conversation page seeds its transcript/header the same way (a chatId switch clears the previous chat's lines immediately instead of letting them linger until the new fetch lands).
- Background-job surfaces poll while pending through `usePollWhile(active, tick, ms, { maxPolls? })` (`components/hooks/use-poll-while.ts`) — the tick is latest-ref'd so a fresh closure never restarts the interval, and `maxPolls` is the safety cap (e.g. an entity-image generation poll). Don't hand-roll `setInterval` poll loops.
- Modal width is `Dialog`'s `size` prop (`components/ui/dialog.tsx`): `md` (default) for confirmations and one-field forms, `lg` for pickers/reading surfaces, `xl` (desktop-wide, `max-w-4xl`) for field-heavy sheets — the chat **Scenario setup** and per-character **Character sheet** modals. Never override width via a `max-w-*` className: `cx` is a plain join (no tailwind-merge), so the caller's cap and the base cap both land on the element and stylesheet order picks the winner — the old `className="max-w-lg"` overrides silently rendered at `max-w-md`.
- Curated model dropdowns (narrator/agent/chat) render through `ModelSelect` (`components/ui/model-select.tsx`): options come from the shared lists in `lib/narrative-models.ts` / `lib/agent-models.ts`, and a value **outside** the list still renders as its own option (the id as-is) so a legacy/env-override id is never silently swapped.
