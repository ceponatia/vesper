# Contracts

`src/contracts/` is the pure, IO-free heart of the domain: the registries and validation schemas that everything else checks itself against. It is documented system-by-system, one doc per subfolder of `src/contracts/`.

📖 **Start at [index.md](index.md)** — the navigation hub, with the full reading order and the extension checklist.
