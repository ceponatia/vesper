# <Idea title>

Status: **draft** — parked in [deferred.plan.md](../deferred.plan.md); not
committed work.

Outcome (provisional): <A player | The owner | A developer> can <do something
concrete> so that <observable consequence>.

## The idea

Two or three sentences. What we would build and what it would change for a
person using the app.

## Why it is parked

What is blocking promotion — an unanswered question, a dependency on other work,
or simply that it is not worth doing yet. Be specific; "low priority" is not a
reason a future reader can act on.

## What promotion would require

The smallest set of answers or prerequisites that would turn this into a real
plan. When these are satisfied, this stub becomes `<topic>.plan.md`, gets a
`roadmap.md` line, and leaves the parking lot behind a one-line tombstone.

## Evidence it is real

Any code-verified observation behind the idea — the file, the behavior, the date
it was checked. A parked idea with no evidence is a guess, and should say so.
